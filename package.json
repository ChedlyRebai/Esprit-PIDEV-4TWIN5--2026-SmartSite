{
  "name": "smartsite-platform",
  "private": true,
  "version": "0.0.1",
  "description": "SmartSite — UI dans apps/frontend, microservices Nest dans apps/backend/*",
  "scripts": {
    "dev": "npm run dev --prefix apps/frontend",
    "build": "npm run build --prefix apps/frontend"
  }
}
