describe('SuperAdminSeedService', () => {
  it('should be a bootstrap service', () => {
    expect(true).toBe(true);
  });

  it('should exist as a module', () => {
    expect(true).toBe(true);
  });

  it('should have seed capability', () => {
    expect(true).toBe(true);
  });
});
