import { Test, TestingModule } from '@nestjs/testing';
import { StaticsModule } from './statics.module';

describe('StaticsModule', () => {
  it('should be defined', () => {
    expect(StaticsModule).toBeDefined();
  });
});
